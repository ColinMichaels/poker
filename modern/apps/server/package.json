{
  "name": "@poker/server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node ./src/index.ts"
  },
  "dependencies": {
    "@poker/game-contracts": "workspace:*",
    "@poker/poker-engine": "workspace:*"
  }
}
